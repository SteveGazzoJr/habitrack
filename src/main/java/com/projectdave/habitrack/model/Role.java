package com.projectdave.habitrack.model;

//I'm sure it won't last, but for now making users self-managing is the ideal we shoot for
//so everyone is USER for now
public enum Role {
    ADMIN,
    USER;
}
